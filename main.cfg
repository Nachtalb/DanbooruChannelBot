[buildout]
develop = .
extensions = mr.developer

extends =
    versions.cfg

parts +=
    bot

show-picked-versions = true

versions = versions

[bot]
recipe = zc.recipe.egg
eggs = DanbooruChannelBot
interpreter = py
