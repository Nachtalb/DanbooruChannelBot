services:
  danbooru_channel_bot:
    container_name: danbooru_channel_bot
    build: .
    restart: unless-stopped
    env_file: .env
    # ports:
    #   - 5555:5555  # webhook mode
    volumes:
      - ./config:/config
